import React from 'react'
import { View } from 'react-native'

const Separator = () => {
    return (<View style={{ backgroundColor: 'lightgrey', height: 1, marginBottom: 15 }} />)
}

export default Separator
